// nexus-directory — Phase 2
// This page will eventually query the real nexus-directory API.
// For now, it renders mock instances to illustrate the concept.
export const load = async () => ({ });
